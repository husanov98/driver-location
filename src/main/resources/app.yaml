runtime: java17
env: standard
#runtime_config:
#  jdk: openjdk11env_variables:
#  SPRING_PROFILES_ACTIVE: "production"
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1